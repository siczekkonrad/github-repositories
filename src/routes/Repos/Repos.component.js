import React from 'react'
import ReposList from '../../components/ReposList/ReposList.component';

const Repos = (props) => (
    <div>
        <ReposList {...props} />
    </div>
    
)

export default Repos